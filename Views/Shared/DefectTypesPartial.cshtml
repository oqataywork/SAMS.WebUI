@using Kendo.Mvc.UI;
@using SAMS.Model;
@{
    Layout = "~/Views/Shared/_Layout_partial.cshtml";
}

<script type="text/javascript">
    function DefectTypegriderror_error(e) {
        if (e.errors) {
            var message = "There are some errors:\n";
            // Create a message containing all errors.
            $.each(e.errors, function (key, value) {
                if ('errors' in value) {
                    $.each(value.errors, function () {
                        message += this + "\n";
                    });
                }
            });
            // Display the message.
            Swal.fire({ icon: 'error', text: message, showConfirmButton: true });
            // Cancel the changes.
            var grid = $("#DefectTypegrid").data("kendoGrid");
            grid.cancelChanges();
        }
    }

    function DefectTypegridrequest_end(e) {
        if ((e.type === "create" && e.response.Errors === null) || (e.type === "update" && e.response.Errors === null))
        {
            Swal.fire({ icon: 'success', text: "Əməliyyat uğurla tamamlandı.", showConfirmButton: false, timer: 1500 });
        }
    }

    function getgridTooltip(e) {
        var dataItem = $("#DefectTypegrid").data("kendoGrid").dataItem(e.target.closest("tr"));
        var content = 'Created at: ' + kendo.toString(kendo.parseDate(dataItem.CreateDate), "dd.MM.yyyy HH:mm") + ' Changed at: ' + kendo.toString(kendo.parseDate(dataItem.CreateDate), "dd.MM.yyyy HH:mm") + '<br/>Changed by:' + dataItem.ChangedByFullName;
        return content;
    }


    function onDefectTypegridChange(e) {
        var dataItem = this.dataItem(this.select());
        if(dataItem){
          var id = dataItem.DefectTypeID;
          $("#DefectTypeID").val(id);
        }
    }




    function FunDefectTypeID() {
        var _selId = $("#DefectTypeID").val();
        return {
            defecttypeid: _selId
        };
    }

    function SetDefectTypegrid_edit(arg) {
        if (arg === true) {
            $("#DefectTypegrid_edit_button").hide();
            $("#DefectTypegrid_delete_button").hide();
            $("#charcat_pdf").hide();
            $("#charcat_excel").hide();
            $("#DefectTypegrid_add_button").hide();
            $("#DefectTypegrid_cancel_button").show();
            $("#DefectTypegrid_update_button").show();
        }
        else {
            $("#DefectTypegrid_edit_button").show();
            $("#DefectTypegrid_delete_button").show();
            $("#charcat_pdf").show();
            $("#charcat_excel").show();
            $("#DefectTypegrid_add_button").show();
            $("#DefectTypegrid_cancel_button").hide();
            $("#DefectTypegrid_update_button").hide();
        }
    }

    function AddDefectTypegridrow() {
        $("#DefectTypeID").val('');
        grid = $("#DefectTypegrid").data("kendoGrid");
        grid.addRow();
        SetDefectTypegrid_edit(true);
    }


    $(document).ready(function () {


        $("#DefectTypegrid_edit_button").on("click", function (e) {
            e.preventDefault();
            var grid = $("#DefectTypegrid").getKendoGrid();
            var selectedrow = grid.select()[0];
            if (selectedrow) {
                grid.editRow(selectedrow);
                SetDefectTypegrid_edit(true);
            }
        });

        $("#DefectTypegrid_add_button").on("click", function () {
            AddDefectTypegridrow();
        });

        $("#DefectTypegrid_delete_button").on("click", function () {
            grid = $("#DefectTypegrid").data("kendoGrid");
            var sel = $("#DefectTypegrid").data("kendoGrid").select();
            grid.removeRow(sel);
            SetDefectTypegrid_edit(false);
        });

        $("#DefectTypegrid_update_button").on("click", function () {
            debugger;
            $("#DefectTypegrid").data("kendoGrid").saveChanges();
            SetDefectTypegrid_edit(false);
        });

        $("#DefectTypegrid_cancel_button").on("click", function () {
            $("#DefectTypegrid").data("kendoGrid").cancelChanges();
            SetDefectTypegrid_edit(false);
        });
        SetDefectTypegrid_edit(false);

        $("#DefectTypegrid").kendoTooltip({
            filter: "th", // Select the th elements of the Grid.
            position: "top",
            width: 250,
            content: function (e) {
                // Return the text content of the hovered header.
                return e.target.text();
            }
        }).data("kendoTooltip");
    });


</script>

<body>
    <input type="hidden" name="DefectTypeID" id="DefectTypeID" />
<div>
        @{
            int DefectTypePermission = (int)ViewData["DefectTypePermission"];
        }
        @(Html.Kendo().Grid<DefectTypeModel>()
                        .Name("DefectTypegrid")
                          .ToolBar(toolbar =>
                          {
                          toolbar.Template(@<text>
                                                <div class="toolbar">
                                                    @if (DefectTypePermission >2){           
                                                     <div id="DefectTypegrid_add_button" class="k-button-icontext k-grid-add circleButton">
                                                        <a href="#">
                                                            <span class="inner"></span>
                                                            <i class='far fa-plus-square' style='font-size:18px'></i>
                                                        </a>
                                                    </div>
                                                    }
                                                    @if (DefectTypePermission >3){
                                                    <div id="DefectTypegrid_edit_button" class="k-button-icontext k-grid-edit circleButton">
                                                        <a href="#">
                                                            <span class="inner"></span>
                                                            <i class='far fa-edit' style='font-size:18px'></i>
                                                        </a>
                                                    </div>
                                                    }
                                                    @if (DefectTypePermission ==5){
                                                    <div id="DefectTypegrid_delete_button" class="k-button-icontext k-grid-delete circleButton">
                                                        <a href="#">
                                                            <span class="inner"></span>
                                                            <i class='far fa-trash-alt' style='font-size:18px'></i>
                                                        </a>
                                                    </div>
                                                    }
                                                    <div id="DefectTypegrid_update_button" class="k-button-icontext k-grid-update circleButton">
                                                        <a href="#">
                                                            <span class="inner"></span>
                                                            <i class='far fa-check-circle' style='font-size:18px'></i>
                                                        </a>
                                                    </div>
                                                    <div id="DefectTypegrid_cancel_button" class="k-button-icontext k-grid-cancel circleButton">
                                                        <a href="#">
                                                            <span class="inner"></span>
                                                            <i class='far fa-times-circle' style='font-size:18px'></i>
                                                        </a>
                                                    </div>
                                                </div>
                                            </text>);
                          })
                        .Columns(columns =>
                        {
				                  columns.Bound(p => p.DefectTypeName);
				                  columns.Bound(p => p.Deactivate).ClientTemplate("<input disabled type= 'checkbox' #= Deactivate ? checked='checked' :'' # />").EditorTemplateName("<input type= 'checkbox' #= Deactivate ? checked='checked' :'' # />");
				                  columns.Bound(p => p.CreateDate).Format("{0:dd.MM.yyyy}");
				                  columns.Bound(p => p.ChangeDate).Format("{0:dd.MM.yyyy}");
				                  columns.Bound(p => p.ChangedByFullName);
                          columns.Bound(p => p.DefectTypeID).ClientTemplate(@"<div class='columnicon' style='background-image: url(" + @Url.Content("~/Images/info.jpg") + ");'></div>").Title("Change Info").Width(50);
                        })
                        .Editable(editable => editable.Mode(GridEditMode.InLine))
                        .Groupable()
                        .Resizable(resize => resize.Columns(true))
                        .Selectable(selectable =>
                        {
                            selectable.Mode(GridSelectionMode.Single);
                            selectable.Type(GridSelectionType.Row);
                        })
                        .Sortable(sortable =>
                        {
                            sortable.SortMode(GridSortMode.SingleColumn);
                        })
                        .Navigatable()
                        .Filterable()
                        .Scrollable(scr => scr.Height(600))
                        .Events(ev => ev
                            .Change("onDefectTypegridChange")
                        )
                        .DataSource(dataSource => dataSource
                        .Ajax()
                        .ServerOperation(false)
                        .Events(events => events
                            .Error("DefectTypegriderror_error")
                            .RequestEnd("DefectTypegridrequest_end")
                        )
                        .Read(ac => ac.Action("DefectTypes_Read", "DefectTypes"))
                        .Create(ac => ac.Action("DefectTypes_Create", "DefectTypes"))
                        .Update(ac => ac.Action("DefectTypes_Update", "DefectTypes"))
                        .Destroy(ac => ac.Action("DefectTypes_Destroy", "DefectTypes"))
                        .Model(model =>
                        {
				model.Id(p => p.DefectTypeID); model.Field(p => p.DefectTypeID).Editable(false);
				model.Field(p => p.DefectTypeName);
				model.Field(p => p.Deactivate);
				model.Field(p => p.CreateDate).Editable(false);
				model.Field(p => p.ChangeDate).Editable(false);
				model.Field(p => p.ChangedBy);
				model.Field(p => p.ChangedByFullName).Editable(false);
                        })
                        )
                        )
@(Html.Kendo().Tooltip()
    .For("#DefectTypegrid")
    .Filter("td:nth-child(6)")
    //.ContentTemplateId("tooltiptemplate")
    .ContentHandler("getgridTooltip")
    .Position(TooltipPosition.Top)
    .Width(250)
    .Height(100)
)
    </div>

</body>

