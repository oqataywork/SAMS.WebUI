@model Guid?
@(Html.Kendo().DropDownListFor(m => m)
        .DataValueField("OrganizationID")
        .DataTextField("OrganizationName")
        .ValuePrimitive(true)
        .BindTo((System.Collections.IEnumerable)ViewData["organizationslist"])
)
